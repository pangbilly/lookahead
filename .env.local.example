# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Database (Neon) — required from PR 2 onwards
DATABASE_URL=

# Auth.js — required from PR 2 onwards
AUTH_SECRET=                     # openssl rand -base64 32
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
AUTH_TRUST_HOST=true

# Email (Resend) — required from PR 5 onwards
RESEND_API_KEY=
EMAIL_FROM="Lookahead <onboarding@resend.dev>"

# Anthropic — Phase 2+, not needed for Phase 1
ANTHROPIC_API_KEY=

# Vercel Blob — Phase 2+, not needed for Phase 1
BLOB_READ_WRITE_TOKEN=
